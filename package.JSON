{
  "name": "donas-selenium-project",
  "version": "1.0.0",
  "description": "Proyecto demo: tienda de donas con pruebas Selenium (Node.js + Mocha + Mochawesome)",
  "main": "index.js",
  "scripts": {
    "start": "npx http-server -p 5500",
    "test": "mocha tests/*.test.js --reporter mochawesome --reporter-options reportDir=mochawesome-report,reportFilename=report"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chromedriver": "^115.0.0",
    "mocha": "^10.0.0",
    "mochawesome": "^7.0.0",
    "selenium-webdriver": "^4.0.0"
  }
}